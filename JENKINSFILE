node {
bat 'docker stop myapp;docker rm myapp'
}

agent {
  dockerfile {
    filename 'Dockerfile'
  }
}

node {
bat 'echo "Please visit ";docker inspect -f "{{ .NetworkSettings.Networks.nat.IPAddress }}" myapp'
}
