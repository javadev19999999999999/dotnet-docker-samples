pipeline {
    agent {
        dockerfile {
         filename 'Dockerfile'
        }
    }
    stages {
        stage('Get IP') {
            steps {
                sh 'ifconfig'
            }
        }
    }
}
