pipeline {
    agent {
        dockerfile {
         filename 'Dockerfile'
        }
    }
    stages {
        stage('GetIP') {
           steps {
                ifconfig
            }
        }
    }
}
